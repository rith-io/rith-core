language:
  - python
matrix:
  include:
    - os: linux
      python: 3
      dist:
        - bionic
        - xenial
    - os: osx
      python: 3
install:
  - pip install -r ./requirements/development.txt
script:
  - nosetests -v
